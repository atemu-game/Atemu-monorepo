{
  "name": "atemu-monorepo",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "run:redis": "docker run --name redis -p 6379:6379 -d redis",
    "run:redis:stop": "docker stop redis"
  }
}